# Hydrofoil Active Flight Controller — Simulator

Static single-page simulator. Deployed on Vercel as a static site (no build step).

## Local preview

Open `index.html` directly in a browser, or:

```sh
npx serve .
```

## Deploy

Connected to Vercel — pushes to `main` trigger a deploy.
